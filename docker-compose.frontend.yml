version: "3.8"

services:
    frontend:
        container_name: kubercode-frontend
        build:
            context: .
            dockerfile: Dockerfile
        ports:
            - "3000:3000"
        environment:
            - NEXT_PUBLIC_API_URL=http://kubercode-nginx:8080
            - NEXTAUTH_URL=http://localhost:3000
            - NEXTAUTH_SECRET=58aeddb0d583a
        networks:
            - app_network

networks:
    app_network:
        external: true
